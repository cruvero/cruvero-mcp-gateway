apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mcpgateway.fullname" . }}
  labels:
    {{- include "mcpgateway.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "mcpgateway.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mcpgateway.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "mcpgateway.serviceAccountName" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        seccompProfile:
          type: RuntimeDefault
      {{- if .Values.database.autoMigrate.enabled }}
      initContainers:
        - name: db-migrate
          image: "{{ .Values.image.repository }}:{{ default .Chart.AppVersion .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - /mcpgw
            - migrate
            - --direction
            - up
          env:
            {{- include "mcpgateway.runtimeSecretEnv" . | nindent 12 }}
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      {{- end }}
      containers:
        - name: mcpgw
          image: "{{ .Values.image.repository }}:{{ default .Chart.AppVersion .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: https
              containerPort: {{ .Values.service.ports.https }}
              protocol: TCP
            - name: metrics
              containerPort: {{ .Values.service.ports.metrics }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "mcpgateway.configMapName" . }}
          env:
            {{- include "mcpgateway.runtimeSecretEnv" . | nindent 12 }}
            {{- if .Values.admin.enabled }}
            - name: MCPGW_ADMIN_OIDC_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "mcpgateway.runtimeSecretName" . }}
                  key: MCPGW_ADMIN_OIDC_CLIENT_SECRET
                  optional: true
            - name: MCPGW_ADMIN_SESSION_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "mcpgateway.runtimeSecretName" . }}
                  key: MCPGW_ADMIN_SESSION_KEY
                  optional: false
            {{- end }}
            {{- if .Values.deviceFlow.enabled }}
            - name: MCPGW_DEVICE_FLOW_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "mcpgateway.runtimeSecretName" . }}
                  key: MCPGW_DEVICE_FLOW_CLIENT_SECRET
                  optional: true
            {{- end }}
            {{- range .Values.orchestrate.providers }}
            - name: MCPGW_LLM_{{ .name | upper }}_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "mcpgateway.runtimeSecretName" $ }}
                  key: MCPGW_LLM_{{ .name | upper }}_API_KEY
                  optional: true
            {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: https
              scheme: {{ .Values.probes.scheme | default "HTTPS" }}
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: https
              scheme: {{ .Values.probes.scheme | default "HTTPS" }}
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            {{- if .Values.tls.enabled }}
            - name: tls-certs
              mountPath: /tls
              readOnly: true
            {{- end }}
      volumes:
        - name: tmp
          emptyDir: {}
        {{- if .Values.tls.enabled }}
        - name: tls-certs
          secret:
            secretName: {{ include "mcpgateway.tlsSecretName" . }}
        {{- end }}
